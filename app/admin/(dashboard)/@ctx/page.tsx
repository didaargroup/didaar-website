/**
 * Contextual aside for dashboard home
 * Shows page properties when a page is selected
 */
import { DashboardAsideContent } from "@/components/admin/dashboard-aside-content";

export default function DashboardContextualAside() {
  return <DashboardAsideContent />;
}